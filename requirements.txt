# AstrBot VTuber Plugin Requirements
# 核心依赖
aiohttp>=3.8.0
fastapi>=0.100.0
uvicorn>=0.23.0
websockets>=11.0

# 音频处理 (可选，如果不使用 pydub 可以注释掉)
pydub>=0.25.0

# 可选：句子分割（可以使用简化的 regex 模式）
# pysbd>=0.3.4
# langdetect>=1.0.9
